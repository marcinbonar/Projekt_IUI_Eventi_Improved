import React from 'react';
import { renderDOM } from '../../../utils/renderDOM';
import RegistrationPanel from '../RegistrationPanel';

test('renders LoginPanel component', () => {
  renderDOM(<RegistrationPanel />);

});